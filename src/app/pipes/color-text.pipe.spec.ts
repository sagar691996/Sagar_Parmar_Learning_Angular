import { ColorTextPipe } from './color-text.pipe';

describe('ColorTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorTextPipe();
    expect(pipe).toBeTruthy();
  });
});
