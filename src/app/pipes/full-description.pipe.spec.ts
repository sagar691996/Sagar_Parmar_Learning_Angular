import { FullDescriptionPipe } from './full-description.pipe';

describe('FullDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new FullDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
