import { TitleNamePipe } from './title-name.pipe';

describe('TitleNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleNamePipe();
    expect(pipe).toBeTruthy();
  });
});
